// sauce-crm-daemon · version. Kept in lockstep with the plugin manifest/package
// version (0.3.0 at time of writing). esbuild does not inline this; it is a
// plain constant so both the bundle and the tests read the same value.
export const DAEMON_VERSION = "0.3.0";
