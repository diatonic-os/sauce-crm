{
  "compilerOptions": {
    "baseUrl": ".",
    "inlineSourceMap": true,
    "inlineSources": true,
    "module": "ESNext",
    "target": "ES2020",
    "allowJs": true,
    // "bundler" (vs legacy "node") resolves modern dependency `exports` maps
    // (vitest/vite) so the FULL lib check passes without skipLibCheck. Correct
    // for this project since esbuild does the actual bundling.
    "moduleResolution": "bundler",
    "importHelpers": true,
    "isolatedModules": true,
    "strict": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitOverride": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "lib": ["DOM", "ES5", "ES6", "ES7", "ES2020"],
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.svelte", "sdk/**/*.ts"],
  "exclude": ["node_modules", "main.js"]
}
